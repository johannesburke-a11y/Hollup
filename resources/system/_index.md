# Resources / System

> **System-level resources: sub-rules and operational frameworks.**

## Structure

```
system/
├── _index.md
└── rules/
    ├── _index.md
    └── <rule-slug>.md
```

Sub-rules in `rules/` elaborate on specific topics from `RULES.md` with more detail and examples.
